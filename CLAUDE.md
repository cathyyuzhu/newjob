# CLAUDE.md

本文件是 Claude Code 在 `newjob` 项目中工作时的项目级指令。

## 项目文档一览（各自定位，避免内容重复）

- **spec/mission.md**：为什么做这个项目、目标用户、核心目标、非目标、怎么算做对了。只在项目方向本身变化时才需要改，日常加功能一般不动它。
- **spec/roadmap.md**：功能状态追踪表（已完成 / 计划中 / 未来可能），按条目记录完成日期和涉及文件，不写详细使用说明或技术细节，只写"是什么状态"。
- **spec/tech-solution.md**：技术选型和关键设计决策的"为什么这么做"（架构、数据流、技术栈取舍）。功能的技术实现方式变化时才更新，不写面向使用者的操作步骤。
- **spec/architecture.md**：代码"现在分成几层、每层是什么"的结构快照（UI / API / 编排 / 数据与调度 / LLM 功能），按层列文件和能力，不写"为什么这么分"（那是 tech-solution.md 的事）。新增模块、模块换层、模块废弃时都要回来同步，不需要用户明确说"更新文档"才做。
- **spec/hallucination-defense.md**：面向外部读者的"这个项目怎么防 AI 幻觉"说明——讲的是**判断**（哪几层该做、哪几层明确不做、为什么），不是实现清单。**跟 product-review.md 同类：是快照不是活文档，加了新功能不需要回来同步它**；要重新评估就整体覆盖并更新文首日期。里面的实现细节以 architecture.md 为准、决策理由以 tech-solution.md 为准，这份只做面向外人的叙述，不要往里加行号级的代码细节。
- **spec/product-review.md**：某个时间点的产品评估快照（核心竞争力、主要不足、竞品格局、商业化判断）。**是快照不是活文档，加了新功能不需要回来同步它**；要重新评估就整体覆盖并更新文首日期，不要逐条打补丁改成半新半旧。
- **spec/monetization-plan.md**：把项目开放给外部用户并收费的计划——可行性核算（哪条路不可行、为什么）、分阶段操作手册（每阶段带"达不到就停"的量化门槛）、触达渠道与定价方案。**是活文档**：商业化路径变了要回来改。市场/竞品判断的依据在 product-review.md，不在这里重复；它只回答"要不要卖、怎么卖"，不记录功能状态（那是 roadmap.md）。
- **README.md**（根目录）：面向使用者的操作手册——安装、配置、怎么用、文件说明。只写"现在功能怎么用"，不重复 roadmap 的历史/状态信息，也不重复 tech-solution 的选型理由。
- **config.md**（根目录）：本机开发环境配置记录（Node/Cursor/VPN代理等），跟项目功能本身无关，一般不需要因为聊新功能而改它。

写内容前先判断该进哪份文档，不要把同一段描述抄进多份文档——需要互相引用时用相对链接指过去，而不是复制文字。

## 文档同步规则

- 每次用户和你讨论新功能时（不管是刚提出想法、正在规划、还是已经开始实现），必须同步更新 `spec/roadmap.md`：
  - 刚讨论、还没实现的功能，加入"计划中 / 讨论中"部分，注明讨论日期。
  - 功能开发完成后，从"计划中 / 讨论中"移到"已完成"部分，补充完成日期和涉及的主要文件。
  - 更新文末的"最后更新"日期。
- 功能开发完成后，同步检查是否需要更新 `README.md`（使用方式变了）、`spec/tech-solution.md`（技术选型/架构变了）和 `spec/architecture.md`（新增/挪动/删除了模块，导致分层结构变化），保持所有文档和实际功能一致，不要留下过期信息。
- 这是每次讨论新功能都要执行的默认动作，不需要用户明确说"更新文档"才做。

## 代码设计原则

### Agent 必须做成无状态归约器（stateless reducer）

新增或修改任何"LLM 自主决策 + 多步工具调用"的 agent 逻辑时，照抄项目里现成的范式，不要另起一套：

- **单步决策函数必须无状态**：签名形如 `(到目前为止的完整状态, 可选工具, ...) → 这一步的动作`。函数内部不能用实例变量/全局变量/闭包记"现在是第几轮""上一步做了什么"——每次调用都是独立的，只根据传进来的状态计算下一步。
- **状态由调用方持有和更新**，不下沉进决策函数：调用方负责维护消息/状态列表，每轮把模型的回复和工具执行结果 append 回去，再整份传入下一次调用。
- **循环控制留在调用方**：要跑几轮、什么时候判定结束、异常中止条件，都写在调用方的循环体里，决策函数本身不知道也不关心自己被调用了多少次。
- **现成范例**：`llm.py` 的 `chat_tool_step()` 是归约器本身（无状态，纯粹"喂历史进去、吐一步决定出来"）；`linkedin_how_you_fit.py` 的 `_scan_with_agent()` 是状态持有者和循环控制方（维护 `messages` 列表、判断步数上限和终止条件）。详见 [spec/architecture.md](spec/architecture.md) 第5层说明。
- **为什么这么做**：单步逻辑能独立测试，不需要搭一整台多轮对话的状态机去 mock（见 `tests/test_llm_tool_use.py`）；切换 LLM provider 不影响调用方的循环代码；决策函数和业务规则（何时停、何时报错）职责分离。
- 以后新增第二个 agent 循环时，新的归约器函数放 `llm.py`，状态列表和循环控制留给新的调用方模块自己管——不要在 `llm.py` 里为新 agent 再造一套"记住上一轮"的内部状态。
